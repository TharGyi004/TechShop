services:
  - type: web
    name: techshop-web
    env: dotnet-core
    branch: master
    buildCommand: 'dotnet publish -c Release -o ./out'
    startCommand: 'dotnet ./out/TechShop.dll'
    plan: free
